//! Shared wire types, domain types and the `Provider` trait.
//!
//! PLACEHOLDER. The agent assigned to author this crate produced nothing while
//! three other crates each defined their own local copies of these types. Per
//! `docs/DIVERGENCES.md`, the most complete of those is promoted here during the
//! integration pass (`plans/C11-integration.md`) rather than authoring a fourth
//! definition.
//!
//! This file exists only so the workspace manifest parses — without a target,
//! `cargo` refuses to load the workspace and every crate is blocked.
