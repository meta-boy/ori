# Placeholder manifest — owned by C4 (proxmox) / C7 (providers-extra). Replace
# freely; do not add source here until you own this crate.
[package]
name = "ori-providers"
version.workspace = true
edition.workspace = true
