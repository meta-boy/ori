# infra/env/caddy.env.example -> copy to /etc/caddy/edge.env (bootstrap.sh does this)
# The Caddyfile *requires* these two; an empty value breaks validation.
EDGE_DOMAIN=ori.local
CONTROL_PLANE_BASE=http://127.0.0.1:8080